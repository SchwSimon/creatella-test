import * as types from '../../actions/action-types';

describe('Action types', () => {
  it('GENERATE_QUERY_VALUE', () => {
    expect(types.GENERATE_QUERY_VALUE).toBe('ADS/GENERATE_QUERY_VALUE');
  });
});
